# 🔒 Решение проблемы SSL сертификата GigaChat API

## Проблема

```
net::ERR_CERT_AUTHORITY_INVALID
```

GigaChat API использует самоподписанные SSL сертификаты, которые блокируются браузером.

## ✅ Решение 1: Использование прокси (Рекомендуется)

Я уже настроил прокси в `vite.config.js`. Просто перезапустите сервер:

```bash
# Остановите: Ctrl+C
# Запустите снова:
npm run dev
```

Прокси автоматически:
- ✅ Обходит CORS ограничения
- ✅ Игнорирует проверку SSL сертификатов
- ✅ Работает только в режиме разработки
- ✅ Безопасно для локального использования

## ✅ Решение 2: Разрешить сертификат в браузере

### Chrome/Edge:

1. Откройте в новой вкладке:
   ```
   https://ngw.devices.sberbank.ru:9443/api/v2/oauth
   ```

2. Увидите предупреждение "Your connection is not private"

3. Нажмите **"Advanced"** → **"Proceed to ngw.devices.sberbank.ru (unsafe)"**

4. Повторите для:
   ```
   https://gigachat.devices.sberbank.ru/api/v1
   ```

5. Вернитесь к приложению и обновите страницу

### Firefox:

1. Откройте ссылку выше
2. Нажмите **"Advanced"** → **"Accept the Risk and Continue"**
3. Повторите для второй ссылки
4. Обновите приложение

## ✅ Решение 3: Запуск Chrome с отключенной проверкой SSL

⚠️ **ТОЛЬКО ДЛЯ РАЗРАБОТКИ!**

### Windows:

```powershell
# Закройте все окна Chrome
# Запустите с флагом:
"C:\Program Files\Google\Chrome\Application\chrome.exe" --ignore-certificate-errors --ignore-urlfetcher-cert-requests --user-data-dir="C:\chrome-dev-session"
```

### Или создайте ярлык:

1. Правый клик на рабочем столе → Создать → Ярлык
2. Путь:
   ```
   "C:\Program Files\Google\Chrome\Application\chrome.exe" --ignore-certificate-errors --user-data-dir="C:\chrome-dev"
   ```
3. Название: **Chrome Dev (No SSL)**
4. Используйте этот ярлык для разработки

## 🔍 Проверка работы

После применения любого решения:

1. Откройте http://localhost:3000
2. Откройте консоль разработчика (F12)
3. Должны увидеть:
   ```
   ✅ GigaChat authentication successful
   ```
4. Статус API в правом нижнем углу: 🟢 **"Подключено к GigaChat"**

## 🎯 Текущая конфигурация

Приложение автоматически:
- ✅ Использует прокси в режиме разработки
- ✅ Прямые запросы в продакшене
- ✅ Обработка ошибок SSL
- ✅ Fallback на демо-режим при ошибках

## 📋 Файлы с изменениями

- ✅ `vite.config.js` - настроен прокси
- ✅ `src/services/gigachatService.js` - использует прокси в dev режиме
- ✅ `.env` - ваши credentials настроены

## 🚀 Быстрый старт

Самый простой способ:

```bash
# 1. Перезапустите сервер
npm run dev

# 2. Подождите загрузки

# 3. Если всё ещё ошибка - разрешите сертификаты в браузере (Решение 2)

# 4. Проверьте статус API в приложении
```

## ⚠️ Важно для продакшена

В продакшене:
- НЕ используйте `--ignore-certificate-errors`
- Используйте валидные SSL сертификаты
- Настройте CORS на сервере
- Используйте переменные окружения для credentials

## 💡 Альтернатива: Демо-режим

Если SSL проблемы не решаются, приложение автоматически работает в демо-режиме:
- ✅ Все функции доступны
- ⚠️ Ответы заготовленные, не от реальной AI
- ✅ Хорошо для демонстрации и тестирования UI

Статус будет: 🟡 **"Демо-режим"**

---

**После перезапуска сервера проблема должна быть решена через прокси!** 🎉

