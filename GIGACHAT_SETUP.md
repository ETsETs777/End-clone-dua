# 🤖 Настройка GigaChat API

## Обзор

Приложение теперь полностью интегрировано с настоящим GigaChat API от Сбербанка! Это означает, что вы получаете доступ к мощной нейросети для изучения английского языка.

## 📋 Что вам понадобится

Учетные данные GigaChat API:
- **Client ID** (UUID)
- **Client Secret** (UUID)
- **Authorization Token** (Base64 строка)
- **Scope** (обычно `GIGACHAT_API_PERS`)

## 🔑 Получение учетных данных

### 1. Регистрация в GigaChat

1. Перейдите на портал GigaChat: https://developers.sber.ru/gigachat
2. Зарегистрируйтесь или войдите
3. Создайте новое приложение
4. Получите Client ID и Client Secret

### 2. Формирование Authorization Token

Authorization Token создается из Client ID и Client Secret:

```bash
# Формат: CLIENT_ID:CLIENT_SECRET
# Затем кодируется в Base64
echo -n "CLIENT_ID:CLIENT_SECRET" | base64
```

**Пример:**
```
Client ID: 019a81d2-9f7c-7429-a7eb-f240038d4d22
Client Secret: 580f330d-4678-48a3-90b4-7267226dd87e

Строка для кодирования:
019a81d2-9f7c-7429-a7eb-f240038d4d22:580f330d-4678-48a3-90b4-7267226dd87e

После Base64:
MDE5YTgxZDItOWY3Yy03NDI5LWE3ZWItZjI0MDAzOGQ0ZDIyOjU4MGYzMzBkLTQ2NzgtNDhhMy05MGI0LTcyNjcyNmRkODc0ZQ==
```

## ⚙️ Настройка приложения

### Шаг 1: Создайте .env файл

В корневой папке проекта создайте файл `.env`:

```bash
# В Windows (PowerShell)
New-Item -Path .env -ItemType File

# Или просто создайте текстовый файл и переименуйте в .env
```

### Шаг 2: Добавьте учетные данные

Откройте `.env` в текстовом редакторе и добавьте:

```env
# GigaChat API Configuration
VITE_GIGACHAT_CLIENT_ID=ваш-client-id
VITE_GIGACHAT_CLIENT_SECRET=ваш-client-secret
VITE_GIGACHAT_AUTH_TOKEN=ваш-base64-токен
VITE_GIGACHAT_SCOPE=GIGACHAT_API_PERS

# GigaChat API Endpoints
VITE_GIGACHAT_AUTH_URL=https://ngw.devices.sberbank.ru:9443/api/v2/oauth
VITE_GIGACHAT_API_URL=https://gigachat.devices.sberbank.ru/api/v1

# Application Configuration
VITE_APP_NAME=English Learning Assistant
VITE_APP_VERSION=1.0.0
```

### Шаг 3: Замените значения

Замените:
- `ваш-client-id` → ваш Client ID
- `ваш-client-secret` → ваш Client Secret
- `ваш-base64-токен` → ваш Authorization Token в Base64

**Пример готового .env:**

```env
VITE_GIGACHAT_CLIENT_ID=019a81d2-9f7c-7429-a7eb-f240038d4d22
VITE_GIGACHAT_CLIENT_SECRET=580f330d-4678-48a3-90b4-7267226dd87e
VITE_GIGACHAT_AUTH_TOKEN=MDE5YTgxZDItOWY3Yy03NDI5LWE3ZWItZjI0MDAzOGQ0ZDIyOjU4MGYzMzBkLTQ2NzgtNDhhMy05MGI0LTcyNjcyNmRkODc0ZQ==
VITE_GIGACHAT_SCOPE=GIGACHAT_API_PERS
VITE_GIGACHAT_AUTH_URL=https://ngw.devices.sberbank.ru:9443/api/v2/oauth
VITE_GIGACHAT_API_URL=https://gigachat.devices.sberbank.ru/api/v1
```

### Шаг 4: Перезапустите сервер

**ВАЖНО:** После создания/изменения `.env` файла необходимо перезапустить сервер разработки:

```bash
# Остановите текущий сервер (Ctrl+C)
# Затем запустите снова:
npm run dev
```

## 🧪 Проверка подключения

### 1. Визуальная проверка

После перезапуска приложения:
1. Откройте http://localhost:3000
2. В правом нижнем углу увидите индикатор статуса API
3. Нажмите на него для деталей

**Статусы:**
- 🟢 **"Подключено к GigaChat"** - всё работает!
- 🟡 **"Настроено, требуется авторизация"** - нажмите "Обновить"
- 🟡 **"Демо-режим"** - API не настроен
- 🔴 **"Не настроено"** - проверьте .env файл

### 2. Тестирование в чате

1. Перейдите в раздел "Чат с ИИ"
2. Отправьте сообщение, например: "Hello, how are you?"
3. Если API настроен правильно, вы получите ответ от настоящей нейросети GigaChat

### 3. Проверка в консоли браузера

Откройте консоль разработчика (F12) и проверьте логи:

```javascript
// Должны увидеть:
✅ GigaChat authentication successful
```

## 🔍 Troubleshooting (Решение проблем)

### Проблема: "Демо-режим" после настройки

**Причина:** `.env` файл не загружается

**Решение:**
1. Убедитесь, что файл называется именно `.env` (без расширения)
2. Файл должен быть в корневой папке проекта (рядом с `package.json`)
3. Перезапустите сервер разработки (`Ctrl+C`, затем `npm run dev`)

### Проблема: "Authentication failed"

**Причина:** Неверные учетные данные

**Решение:**
1. Проверьте правильность Client ID и Client Secret
2. Убедитесь, что Authorization Token правильно закодирован в Base64
3. Проверьте, что между `:` нет пробелов при создании токена
4. Попробуйте пересоздать токен

### Проблема: Токен истекает

**Причина:** Токены GigaChat действуют 30 минут

**Решение:**
- Приложение автоматически обновляет токен
- Если возникают проблемы, нажмите "Обновить" в индикаторе статуса
- При необходимости перезапустите приложение

### Проблема: CORS ошибки

**Причина:** Ограничения безопасности браузера

**Решение:**
1. Убедитесь, что используете HTTPS endpoints
2. Проверьте настройки CORS в вашем приложении GigaChat
3. В разработке можно использовать прокси (см. `vite.config.js`)

### Проблема: SSL сертификаты

**Причина:** Самоподписанные сертификаты

**Решение:**
```javascript
// В разработке можно отключить проверку SSL (НЕ для продакшена!)
// Добавьте в vite.config.js:
server: {
  https: false,
  proxy: {
    '/api': {
      target: 'https://gigachat.devices.sberbank.ru',
      changeOrigin: true,
      secure: false
    }
  }
}
```

## 📊 Мониторинг использования

### Лимиты API

GigaChat API имеет лимиты:
- **Requests per minute (RPM)** - количество запросов в минуту
- **Tokens per request** - максимум токенов на запрос
- **Daily quota** - дневной лимит

Проверяйте ваши лимиты в личном кабинете GigaChat.

### Оптимизация запросов

Приложение оптимизировано:
- ✅ Кэширование токенов (30 минут)
- ✅ Автоматическое обновление токенов
- ✅ История последних 10 сообщений для контекста
- ✅ Fallback на демо-режим при ошибках

## 🔒 Безопасность

### ⚠️ ВАЖНО

1. **НЕ** публикуйте `.env` файл в Git!
   - Файл `.env` уже добавлен в `.gitignore`
   - Никогда не коммитьте учетные данные

2. **НЕ** делитесь своими токенами!
   - Authorization Token дает полный доступ к API
   - Храните его как пароль

3. **Используйте разные токены** для разработки и продакшена

4. **Регулярно меняйте** учетные данные

### Переменные окружения в продакшене

Для деплоя используйте переменные окружения платформы:

**Vercel:**
```bash
vercel env add VITE_GIGACHAT_CLIENT_ID
vercel env add VITE_GIGACHAT_AUTH_TOKEN
```

**Netlify:**
```bash
# В Settings → Environment Variables
```

**Docker:**
```dockerfile
ENV VITE_GIGACHAT_CLIENT_ID=your-id
ENV VITE_GIGACHAT_AUTH_TOKEN=your-token
```

## 🚀 Готово!

После успешной настройки вы получаете:

✅ **Настоящий AI помощник** - ответы от GigaChat нейросети
✅ **Умные диалоги** - контекстные ответы с памятью
✅ **Персонализация** - адаптация под ваш уровень
✅ **Качественные объяснения** - детальные пояснения грамматики
✅ **Естественный язык** - как разговор с преподавателем

## 📚 Дополнительные ресурсы

- [Документация GigaChat API](https://developers.sber.ru/docs/ru/gigachat/api/overview)
- [Примеры использования](https://developers.sber.ru/docs/ru/gigachat/api/examples)
- [FAQ](https://developers.sber.ru/docs/ru/gigachat/faq)
- [Техническая поддержка](https://developers.sber.ru/support)

## 💡 Советы по использованию

1. **Задавайте конкретные вопросы** - AI лучше работает с четкими запросами
2. **Используйте контекст** - ссылайтесь на предыдущие сообщения
3. **Просите примеры** - AI отлично генерирует примеры
4. **Экспериментируйте** - пробуйте разные форматы вопросов

## 🎯 Следующие шаги

После настройки API попробуйте:
- Чат с AI для разговорной практики
- Объяснения грамматики с примерами
- Перевод с контекстом и нюансами
- Генерацию упражнений
- Анализ ваших текстов

---

**Нужна помощь?** Создайте Issue в GitHub или обратитесь в поддержку Sber Developers.


